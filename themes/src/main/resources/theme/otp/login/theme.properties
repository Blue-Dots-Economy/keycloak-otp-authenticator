parent=keycloak.v2

# Blue Dots branding for the Aggregator portal.
# Stylesheets are injected via the local template.ftl override; this file
# pins the parent so default Keycloak v2 partials remain available.
